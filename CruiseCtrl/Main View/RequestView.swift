//
//  RequestView.swift
//  CruiseCtrl
//
//  Created by Henry Webb on 9/19/23.
//

/*
 This view should first tell the user if sober d is going or not... if not it will say when they will be going next, if they are within the "valid hours" then it shows names for driver, and officer.... it will also say how many pppl iin queue and maybe estimated wait time
 
 
 If the user hits the request button at the bottom of the page then the sheet popus up for user info
 */

import SwiftUI

struct RequestView: View {
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
    }
}

#Preview {
    RequestView()
}
